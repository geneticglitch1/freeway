node_modules/
dist/
*.tsbuildinfo

# Secrets and live state — never commit these.
.env
.env.*
!.env.example
data/
!data/.gitkeep
*.db
*.db-wal
*.db-shm

*.log
.DS_Store

# recorder scratch — the encoded demo is committed, the frames are not
demo/frames/
